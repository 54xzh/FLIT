// https://developer.android.com/jetpack/compose/performance/stability/fix#configuration-file

// Kotlin Immutable Collections
kotlinx.collections.immutable.*

kotlin.uuid.*
kotlin.time.*

// Java Time
java.time.*

// 注意：以下业务类型被声明为 stable，等于向 Compose 承诺「equals 相等 ⇒ 无可观察变化」。
// UIMessagePart 各子类含 var metadata：只允许在实例进入组合（被渲染）前写入，
// 禁止对已渲染实例原地修改，否则会出现仅发布版可复现的界面不刷新问题。
me.rerere.rikkahub.data.model.Conversation
me.rerere.rikkahub.data.model.MessageNode
me.rerere.ai.ui.UIMessage
me.rerere.ai.ui.UIMessagePart
me.rerere.ai.ui.UIMessagePart.*
me.rerere.ai.ui.UIMessageChoice
me.rerere.ai.ui.MessageChunk
me.rerere.ai.ui.UIMessageAnnotation
me.rerere.ai.ui.ToolApprovalState
me.rerere.ai.core.TokenUsage

// Settings 整体经 StateFlow 全量替换、从不原地修改；声明 stable 让接收它的组合函数可跳过。
me.rerere.rikkahub.data.datastore.Settings
me.rerere.rikkahub.data.datastore.DisplaySetting